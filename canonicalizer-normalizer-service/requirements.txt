# Core dependencies
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Kafka
confluent-kafka>=2.3.0
cachetools>=5.3.0  # Required by confluent-kafka schema registry
authlib>=1.2.0  # Required by confluent-kafka schema registry
fastavro>=1.8.0  # Required by confluent-kafka schema registry

# Database
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Cache
redis>=5.0.1

# URL processing
tldextract>=5.1.1
urllib3>=2.1.0

# Text processing
ftfy>=6.1.1
beautifulsoup4>=4.12.2
datasketch>=1.6.5
textstat>=0.7.3

# Language-specific processing (optional, graceful fallback if not available)
hazm>=0.7.0  # Persian text processing
opencc-python-reimplemented>=0.1.7  # Chinese text processing
python-bidi>=0.4.2  # Arabic text processing
arabic-reshaper>=0.6.0  # Arabic text reshaping

# ML-based classification (optional, graceful fallback if not available)
transformers>=4.35.0  # Transformer models for classification
torch>=2.1.0  # PyTorch for ML models
sentence-transformers>=2.2.0  # Sentence embeddings for semantic similarity
scikit-learn>=1.3.0  # Machine learning utilities

# Language detection (optional, graceful fallback if not available)
langdetect>=1.0.9  # Language detection library

# Sentiment analysis (optional, graceful fallback if not available)
nltk>=3.8.1  # VADER sentiment analysis for English
textblob>=0.17.1  # Simple sentiment analysis fallback

# HTTP client for redirect resolution (optional, graceful fallback if not available)
httpx>=0.24.0  # Async HTTP client

# Distributed tracing (optional, graceful fallback if not available)
opentelemetry-api>=1.20.0  # OpenTelemetry API
opentelemetry-sdk>=1.20.0  # OpenTelemetry SDK
opentelemetry-exporter-jaeger>=1.20.0  # Jaeger exporter

# Metrics
prometheus-client>=0.19.0

# Logging and tracing
python-json-logger>=2.0.7

# HTTP client
httpx>=0.25.2
aiohttp>=3.9.1

# Utilities
python-ulid>=1.1.0
pytz>=2023.3

# Development and testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
pytest-mock>=3.12.0
black>=23.12.0
ruff>=0.1.8
mypy>=1.7.1

