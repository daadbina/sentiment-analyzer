apiVersion: v1
kind: Service
metadata:
  name: {{ include "crawler-service.fullname" . }}
  labels:
    {{- include "crawler-service.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "crawler-service.selectorLabels" . | nindent 4 }}

