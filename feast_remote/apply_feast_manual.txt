=== Manual Steps to Apply Feast Feature Definitions ===

The features.py and apply_features.sh files have been copied to the remote server.

Please execute the following commands manually:

1. SSH to the remote server:
   ssh root@154.53.166.231
   Password: MBcH5LubNVSK*

2. Navigate to the feature repository:
   cd ~/feast/feature_repo

3. Make the script executable:
   chmod +x apply_features.sh

4. Run the script:
   ./apply_features.sh

Expected output:
- ✓ Found features.py
- ✓ Feast server container is running
- Applying features to Feast registry...
- Created entity semantic_group
- Created feature view semantic_group_features
- Registered 24 features
- ✓ Feature definitions applied successfully!

Alternative: Run commands directly
If the script doesn't work, run these commands manually:

cd ~/feast/feature_repo
docker exec feast-feature-server feast apply
docker exec feast-feature-server feast feature-views list
docker exec feast-feature-server feast feature-views describe semantic_group_features

Verification:
- Check Feast server health: curl http://154.53.166.231:6566/health
- List features in Redis: docker exec feast-redis redis-cli KEYS "feast:*"

After completion, please confirm that:
- Feature view "semantic_group_features" is listed
- 24 features are registered
- No errors in the output

