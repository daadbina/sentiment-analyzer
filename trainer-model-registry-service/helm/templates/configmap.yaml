apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "trainer-model-registry.fullname" . }}-config
  labels:
    {{- include "trainer-model-registry.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

